import React from 'react';

// Class for Exec Page Layout

class Exec extends React.Component {
    render = () => (
        <div>
            <div>
                Exec
            </div>
        </div>
    )
}

export default Exec;
